# 西煲 (Xibao) — Agent 工作约定

本文件是给 AI 开发助手（及协作会话）的**强制工作约定**，新对话开始时优先阅读。

## 强制工作原则（同等优先级，缺一不可）

1. **先查 GitHub 再动手**：任何新功能/重构实现前，必须先检索 GitHub 找现成方案（能复用不重写）。参考 `DEVELOPMENT.md` 的"工作原则"。
2. **功能变更必须告知**：任何改动导致以下情况，**必须在完成时明确告知用户**：
   - 某个**功能被移除**（包括被重构取代、被合并、被废弃的入口）
   - 某个**功能的行为被改变**（语义变化、交互变化、边界行为变化）
   - 某个**接口/API 被改或删**
   - **数据存储格式变化**（schema 迁移、字段语义变化）
   - 用户可感知的**性能/资源变化**
   告知时要说明：改了什么、为什么改、旧行为如何、新行为如何。不要"润物细无声"地删功能——即使觉得合理，也要先说。

## 项目背景速览

- Windows 本地文件管理器（Flask + 原生 JS + jsTree + jQuery + SortableJS），Python 3.13，SQLite。
- 当前版本 v0.6.0（重构进行中），详见 `DEVELOPMENT.md` 和 `ROADMAP.md`。
- 开发接续上下文、技术栈、关键代码位置、打包流程见 `DEVELOPMENT.md`（新对话先读它）。
- 版本规划、决策记录见 `ROADMAP.md`。

## 常规约定

- 每完成一步 commit 留痕，commit message 写清做了什么。
- 新功能/重构完成后更新接力文档（`DEVELOPMENT.md` / `ROADMAP.md`）。
- 所有改动跑通 `pytest`（`tests/` 全量）再提交。
