# 西煲 版本前瞻

> 本文件记录已定案但尚未实现的未来版本规划（供设计与决策溯源）。
> 当前已发布版本：v0.5.4
> 注：本文件（及 README/DEVELOPMENT.md）在 v0.6.0 英文界面适配时同步做中英双语。

## v0.5.5（规划中）

### 1. 标签树标签后显示内含数量

- 每个标签节点后显示当前标签含有的条目数
- 后端：聚合 `folder_tags` 按 tag_id 计数
- 前端：jsTree 渲染时标签名后加数量
- **口径待定**：直接数（本标签直接挂载）vs 含子级总数（递归合计）
  - 倾向含子级总数（与"点父级筛选含子级"的语义一致）
- 数量实时性：打标/清除后随 `loadTags` 刷新

### 2. 文件评分系统（完整设计）

**核心约束**
- 只能给**已打标签**的条目评分
- 评分逻辑建立在"标签筛选结果"之上（不允许纯评分筛选）
- 评分数据生命周期**完全跟随标签**：
  - 删除文件 → 清评分
  - 移动/重命名 → 受"标签迁移开关"控制（开=跟随，关=清理）
  - 随标签备份导出/导入
- 评分不进筛选方案（方案只管标签组合）

**打分（写入）**
- 入口：快捷键 `W` + 右键菜单「评分…」
- 浮窗：五颗星槽，可点左/右半，0.5 粒度（5 星=10 档），最低半星
- 浮窗含「清除评分」按钮
- 多选时：批量给所有选中条目设同一分数
- 只能对有标签条目弹出（无标签条目无评分入口）

**显示**
- 条目左上角常显角标 `3.5⭐`
- 工具栏显示评分开关 + 快捷键 `TAB` 切换角标显示/隐藏（默认显示）

**筛选（读取）**
- 入口：⭐ 开关，位置在筛选模式 ➕ 按钮与 📚 方案按钮之间
- 仅在 🔖 标签筛选模式激活后可用（评分筛选依赖标签筛选）
- 点击展开面板（停靠在筛选区，不遮挡文件列表）：
  - 星槽选星级（0.5 粒度）
  - 文件 / 文件夹 两个复选框（可只文件、只文件夹、都勾）
- 设置后收起，筛选胶囊条追加状态显示 `★≥4 · 仅文件`
- 逻辑：先标签筛选 → 再评分叠加 → 再类型过滤
- 星级为 ≥ 阈值（选 4 星 = 4 星及以上）
- 未评分条目默认 0 分，不满足 ≥ 阈值时被过滤

**排序**
- 列表排序菜单新增「评分」维度（按星级降序）

**存储**
- 新增 `path_ratings(path, rating)` 表（平行于 folder_tags / path_aliases）
- 跟随标签的删除/移动/备份逻辑

## 未来计划

### 标签名搜索

- 标签树顶部加搜索框，输入子串实时高亮/过滤匹配的标签节点
- 优先级高于"标签别名"（子串模糊匹配覆盖大部分别名需求）

## v0.6.0（规划中）

按优先级排序：

1. **稳定文件标识**：用 NTFS 文件 ID / volume+file index 作标签锚点，路径只是当前地址——解决"文件移动/重命名后标签丢失"的根治方案
2. **数据库容错**：启动完整性校验（PRAGMA integrity_check）、损坏自动修复、定时快照可回滚
3. **层级展开重构**：不物化祖先链，查询期 BFS 展开 + flat_dict 缓存（借鉴 etiquette）——治本"属性父级冗余/叶子升父级幽灵标签"
4. **模块解耦**：app.py 按域拆分（api_tags/api_files/api_search/api_settings），前端按域聚合
5. **开放外部标签写入 API**：`POST /api/external/tags`，定义对外标准，供插件/第三方接入
6. **标签筛选返回交互**：点标签进历史，返回回目录；多标签组合的进史策略
7. **增量更新**（含检查更新）：文件清单/哈希、独立更新器、失败回滚，重构时一并规划发布体系
8. **英文界面适配**：i18n 界面文案 + 帮助文档 + 更新日志，设置加语言选项（中文/English）；重构时顺手 i18n 化，避免稳定版大改

## 远期

- **可视化统计**：标签使用量排行 / 标签树热力 / 标签共现分析（后端聚合 API + 前端图表），等 v0.6.0 数据模型稳定后做
- **视觉识别插件**（BYOK）：独立分发，内部调 Ollama/云端 API 识别，经外部 API 写入西煲；先极简 CLI 验证架构，再升级 GUI
  - 参考：baessu/eagle-auto-tagger（标签收敛）、Duelion/homebox-companion（BYOK/限流）

## 已否定（决策记录，避免重复讨论）

- **标签备注元信息**：个人本地使用，标签名即描述，无用
- **标签模板**：与筛选方案同构冗余，多选追加标签已覆盖
- **筛选结果导出 CSV**：个人使用场景低频
- **按标签归档物理文件**（独立工具也不做）：多标签文件只能放一个物理位置，与"一个文件多标签"核心特性冲突，且违反"不搬移文件"定位
- **评分进筛选方案**：方案管标签，评分是临时叠加层，解耦
